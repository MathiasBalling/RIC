#include "bug1.hpp"
#include <opencv2/core/mat.hpp>
void bug1(const cv::Mat &workspace, const cv::Point &start_pos,
          const cv::Point &end_pos, cv::Mat &img_final) {
  // Clone the edges image
  (void)workspace;
  (void)start_pos;
  (void)end_pos;
  (void)img_final;
}
